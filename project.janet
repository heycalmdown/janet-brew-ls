(declare-project
    :name "brew-ls" # required
    :description "brew ls" # some example metadata.

    # Optional urls to git repositories that contain required artifacts.
    :dependencies ["json" "https://github.com/joy-framework/http"] ["uuid" "https://github.com/joy-framework/uuid"])

